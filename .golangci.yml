version: "2"
linters:
  enable:
    - gosec
    - misspell
    - unconvert
  settings:
    errcheck:
      exclude-functions:
        - (io.Closer).Close
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
